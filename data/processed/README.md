# Processed data

This directory stores generated analysis files created by:

- `scripts/01_clean_qlfs_2023.R`
- `scripts/02_clean_lmdsa_2023.R`

The cleaned CSV files are excluded from the public GitHub version of the project because they are large derived artifacts. Regenerate them locally after placing the required raw data in `data/raw/`.
